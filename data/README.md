# Data Folder
This folder contains raw and processed datasets related to manufacturing and quality control.  
# Reminder 
Use subfolders like `data/raw` and `data/processed` to organize inputs and outputs.
